diff --git a/src/features/tasks/components/expanded-row/production/task-production-panel.tsx b/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
index 209c447..f3bccfd 100644
--- a/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
+++ b/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
@@ -63,7 +63,6 @@ import {
   workflowStepperStyles,
 } from "@/features/workflow/styles/workflow-stepper"
 
-import { getFinishMaterialSurface } from "@/shared/utils/material-surface"
 
 type Props = {
   task: Task
@@ -382,12 +381,9 @@ export function TaskProductionPanel({
             <button
               type="button"
               onClick={() => setExpanded(true)}
-              className="flex h-[70.5px] w-full items-center justify-between rounded-2xl px-4 text-left transition hover:brightness-110 shadow-sm overflow-hidden"
+              className="flex h-[70.5px] w-full items-center justify-between rounded-2xl pr-4 text-left transition hover:brightness-110 shadow-sm overflow-hidden"
               style={{
-                background: getFinishMaterialSurface(
-                  hasPaintProcess ? (task.color?.color ?? "#64748B") : "#BBBBBB",
-                  -24 // Perilla: negativo mueve a la izquierda, positivo a la derecha
-                ),
+                background: "#17191d",
               }}
             >
               <div className="min-w-0 flex-1 overflow-hidden h-full flex items-center">
diff --git a/src/features/tasks/components/expanded-row/task-kpis-section.tsx b/src/features/tasks/components/expanded-row/task-kpis-section.tsx
index 69654d8..df80510 100644
--- a/src/features/tasks/components/expanded-row/task-kpis-section.tsx
+++ b/src/features/tasks/components/expanded-row/task-kpis-section.tsx
@@ -31,6 +31,10 @@ import {
 } from "@/shared/responsive/hooks/use-responsive"
 
 import { getBadgeColors } from "@/shared/utils/badge-colors"
+import {
+  FINISH_BADGE_DARK,
+  getFinishMaterialSurface,
+} from "@/shared/utils/material-surface"
 
 type Props = {
   task: Task
@@ -236,21 +240,33 @@ export function TaskKpisSection({
     const finishInk = getBadgeColors(dominantColor, "solid").text
 
     return (
-      <div className="flex h-full w-full items-center">
-        {/* Bloque de pintura (mantiene su color dinámico sobre el badge) */}
+      <div className="flex h-full min-w-0 items-center">
+        {/*
+          Zona RAL + separador: 0–100% del degradado = este bloque.
+          Sin overflow-hidden (evita el corte rectangular).
+          90deg + stop final = FINISH_BADGE_DARK → empalme continuo.
+        */}
         <div
-          className="flex shrink-0 items-center gap-2.5 pl-2 text-[13px] font-bold tracking-wide"
-          style={{ color: finishInk }}
+          className="relative flex h-full shrink-0 items-center"
+          style={{
+            background: getFinishMaterialSurface(dominantColor),
+          }}
         >
-          <PaintIcon className="h-4 w-4 shrink-0" />
-          <span className="truncate">{paintLabel}</span>
-        </div>
+          <div
+            className="relative z-10 flex shrink-0 items-center gap-2.5 pl-4 pr-8 text-[13px] font-bold tracking-wide"
+            style={{ color: finishInk }}
+          >
+            <PaintIcon className="h-4 w-4 shrink-0" />
+            <span className="truncate">{paintLabel}</span>
+          </div>
 
-        {/* Separador vertical en blanco con opacidad usando tokens */}
-        <div className="mx-8 h-4 w-px shrink-0 bg-white/20" />
+          <div
+            aria-hidden
+            className="relative z-10 mr-1 h-4 w-px shrink-0 bg-white/20"
+          />
+        </div>
 
-        {/* Bloque de producción/material en blanco */}
-        <div className="flex min-w-0 items-center gap-2.5 truncate text-[13px] font-bold tracking-wide text-white">
+        <div className="flex min-w-0 items-center gap-2.5 truncate pl-8 pr-2 text-[13px] font-bold tracking-wide text-white">
           <Layers3 className="h-4 w-4 shrink-0 text-white" />
           <span className="truncate">{materialLabel}</span>
         </div>
diff --git a/src/shared/utils/material-surface.ts b/src/shared/utils/material-surface.ts
index f3d1910..1160386 100644
--- a/src/shared/utils/material-surface.ts
+++ b/src/shared/utils/material-surface.ts
@@ -1,21 +1,23 @@
-export function getFinishMaterialSurface(finishHex: string, shift: number = 0): string {
-  const c = finishHex.trim() || "#64748B"
+/** Oscuro del badge — debe coincidir con el stop final del surface. */
+export const FINISH_BADGE_DARK = "#17191d"
 
-  // Rango más estrecho/pequeño (la transición total pasa de abarcar ~26% a solo ~12%)
-  const b1 = 32 + shift
-  const b2 = 36 + shift // Inicio rápido de la mezcla
-  const b3 = 40 + shift // Sigue oscureciendo rápido
-  const b4 = 44 + shift // Negro total más cerca
+/**
+ * Degradado de la zona RAL + separador.
+ * 0–100% = solo ese bloque (no el badge). 90deg para que el borde
+ * derecho sea uniforme #17191d y empalme sin corte con el padre.
+ */
+export function getFinishMaterialSurface(finishHex: string): string {
+  const c = finishHex.trim() || "#64748B"
+  const d = FINISH_BADGE_DARK
 
   return [
     `linear-gradient(
-      105deg,
+      90deg,
       ${c} 0%,
-      ${c} ${b1}%,
-      color-mix(in srgb, ${c} 65%, #17191d) ${b2}%,
-      color-mix(in srgb, ${c} 28%, #17191d) ${b3}%,
-      #17191d ${b4}%,
-      #17191d 100%
+      ${c} 38%,
+      color-mix(in srgb, ${c} 72%, ${d}) 58%,
+      color-mix(in srgb, ${c} 35%, ${d}) 76%,
+      ${d} 100%
     )`,
     `linear-gradient(
       180deg,
@@ -27,4 +29,4 @@ export function getFinishMaterialSurface(finishHex: string, shift: number = 0):
     .join(", ")
     .replace(/\s+/g, " ")
     .trim()
-}
\ No newline at end of file
+}
