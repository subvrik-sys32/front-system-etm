diff --git a/src/app/(protected)/ingenieria/page.tsx b/src/app/(protected)/ingenieria/page.tsx
index 0e066a8..d40bdba 100644
--- a/src/app/(protected)/ingenieria/page.tsx
+++ b/src/app/(protected)/ingenieria/page.tsx
@@ -1,23 +1,23 @@
 "use client"
 
-import { useSearchParams } from "next/navigation"
-
 import { EngineeringPageContent } from "@/features/engineering/components/engineering-page-content"
 import { usePageTitle } from "@/shared/responsive/navigation/hooks/use-page-title"
 import { PageShell } from "@/shared/responsive/layout/page-shell"
+import { useCaptureDeepLink } from "@/shared/hooks/use-capture-deep-link"
+import { useFocusIntentStore } from "@/shared/focus/store/focus-intent-store"
 
 export default function IngenieriaPage() {
   usePageTitle("Ingeniería")
-  const searchParams = useSearchParams()
-  const taskId = searchParams.get("taskId") ?? undefined
-  const focusToken = searchParams.get("focus") ?? undefined
+
+  useCaptureDeepLink()
+  const intent = useFocusIntentStore(s => s.intent)
 
   return (
     <PageShell mode="list">
       <section className="flex min-h-0 w-full flex-1 flex-col">
         <EngineeringPageContent
-          focusedTaskId={taskId}
-          focusToken={focusToken}
+          focusedTaskId={intent?.taskId}
+          focusToken={intent?.focusToken}
         />
       </section>
     </PageShell>
diff --git a/src/app/(protected)/processes/page.tsx b/src/app/(protected)/processes/page.tsx
index e50a9d3..4186724 100644
--- a/src/app/(protected)/processes/page.tsx
+++ b/src/app/(protected)/processes/page.tsx
@@ -7,12 +7,17 @@ import { getProcessDefinition } from "@/features/processes/selectors/get-process
 import type { ProcessCode } from "@/features/tasks/types/task.types"
 import { usePageTitle } from "@/shared/responsive/navigation/hooks/use-page-title"
 import { PageShell } from "@/shared/responsive/layout/page-shell"
+import { useCaptureDeepLink } from "@/shared/hooks/use-capture-deep-link"
+import { useFocusIntentStore } from "@/shared/focus/store/focus-intent-store"
 
 export default function ProcessPage() {
   const searchParams = useSearchParams()
 
-  const taskId = searchParams.get("taskId") ?? undefined
-  const focusToken = searchParams.get("focus") ?? undefined
+  useCaptureDeepLink()
+  const intent = useFocusIntentStore(s => s.intent)
+
+  const taskId = intent?.taskId
+  const focusToken = intent?.focusToken
   const initialShowHistory = searchParams.get("history") === "1"
   const codeParam = searchParams.get("code") ?? "ct"
   const processCode = codeParam.toUpperCase() as ProcessCode
@@ -33,4 +38,4 @@ export default function ProcessPage() {
       </section>
     </PageShell>
   )
-}
\ No newline at end of file
+}
diff --git a/src/app/(protected)/projects/page.tsx b/src/app/(protected)/projects/page.tsx
index db31013..a9ad129 100644
--- a/src/app/(protected)/projects/page.tsx
+++ b/src/app/(protected)/projects/page.tsx
@@ -8,26 +8,27 @@ import { usePageTitle } from "@/shared/responsive/navigation/hooks/use-page-titl
 import { usePageActions } from "@/shared/responsive/navigation/hooks/use-page-actions"
 import { useResponsive } from "@/shared/responsive/hooks/use-responsive"
 import { PageShell } from "@/shared/responsive/layout/page-shell"
+import { useCaptureDeepLink } from "@/shared/hooks/use-capture-deep-link"
+import { useFocusIntentStore } from "@/shared/focus/store/focus-intent-store"
 
 export default function ProjectsPage() {
   usePageTitle("Proyectos")
   const { isMobile } = useResponsive()
   usePageActions(isMobile ? null : <ProjectActions />)
 
+  useCaptureDeepLink()
+  const intent = useFocusIntentStore(s => s.intent)
   const searchParams = useSearchParams()
-  const projectId = searchParams.get("projectId") ?? undefined
-  const focusToken = searchParams.get("focus") ?? undefined
-  const initialShowHistory = searchParams.get("history") === "1"
 
   return (
     <PageShell mode="list">
       <section className="flex min-h-0 w-full flex-1 flex-col">
         <ProjectPageContent
-          focusedProjectId={projectId}
-          focusToken={focusToken}
-          initialShowHistory={initialShowHistory}
+          focusedProjectId={intent?.projectId}
+          focusToken={intent?.focusToken}
+          initialShowHistory={searchParams.get("history") === "1"}
         />
       </section>
     </PageShell>
   )
-}
\ No newline at end of file
+}
diff --git a/src/app/(protected)/tasks/page.tsx b/src/app/(protected)/tasks/page.tsx
index e675179..2c88107 100644
--- a/src/app/(protected)/tasks/page.tsx
+++ b/src/app/(protected)/tasks/page.tsx
@@ -7,26 +7,26 @@ import { TaskPageContent } from "@/features/tasks/components/task-page-content"
 import { usePageTitle } from "@/shared/responsive/navigation/hooks/use-page-title"
 import { PageShell } from "@/shared/responsive/layout/page-shell"
 import { usePageActions } from "@/shared/responsive/navigation/hooks/use-page-actions"
+import { useCaptureDeepLink } from "@/shared/hooks/use-capture-deep-link"
+import { useFocusIntentStore } from "@/shared/focus/store/focus-intent-store"
 
 export default function TasksPage() {
   usePageTitle("Tareas")
   usePageActions(<TaskActions />)
 
+  useCaptureDeepLink()
+  const intent = useFocusIntentStore(s => s.intent)
   const searchParams = useSearchParams()
-  const taskId = searchParams.get("taskId") ?? undefined
-  const focusToken = searchParams.get("focus") ?? undefined
-  const initialShowHistory = searchParams.get("history") === "1"
 
   return (
     <PageShell mode="list">
-
       <section className="flex min-h-0 w-full flex-1 flex-col">
         <TaskPageContent
-          focusedTaskId={taskId}
-          focusToken={focusToken}
-          initialShowHistory={initialShowHistory}
+          focusedTaskId={intent?.taskId}
+          focusToken={intent?.focusToken}
+          initialShowHistory={searchParams.get("history") === "1"}
         />
       </section>
     </PageShell>
   )
-}
\ No newline at end of file
+}
diff --git a/src/features/processes/components/expanded-row/process-expanded-row.tsx b/src/features/processes/components/expanded-row/process-expanded-row.tsx
index 2226a07..8dfc04a 100644
--- a/src/features/processes/components/expanded-row/process-expanded-row.tsx
+++ b/src/features/processes/components/expanded-row/process-expanded-row.tsx
@@ -28,7 +28,7 @@ import { ProcessTimeCard } from "./cards/process-time-card"
 import { ProcessProgressCard } from "./cards/process-progress-card"
 import { CommentHistoryDialog } from "@/features/comments/components/comment-history-dialog"
 import { useFocusSettleStore } from "@/shared/focus/store/focus-settle-store"
-import { consumeCommentsTabParam } from "@/shared/hooks/consume-comments-tab"
+import { useFocusIntentStore } from "@/shared/focus/store/focus-intent-store"
 
 type Props = {
   processTask: ProcessTask
@@ -48,9 +48,9 @@ export function ProcessExpandedRow({
   const router = useRouter()
   const pathname = usePathname()
 
-  const urlTaskId = searchParams.get("taskId")
-  const isTarget = urlTaskId === processTask.task.id
-  const tabParam = searchParams.get("tab") as "comments" | "kpis"
+  const intent = useFocusIntentStore(s => s.intent)
+  const isTarget = intent?.taskId === processTask.task.id
+  const tabParam = intent?.tab
 
   const processCode =
     processTask.workflowStep?.processCode
@@ -168,7 +168,7 @@ export function ProcessExpandedRow({
     setCommentsDialogOpen,
   ] = useState(false)
 
-  const urlFocusToken = searchParams.get("focus")
+  const urlFocusToken = intent?.focusToken ?? null
   const settledToken = useFocusSettleStore(s => s.settledToken)
   const focusSettled = !urlFocusToken || settledToken === urlFocusToken
 
@@ -184,8 +184,8 @@ export function ProcessExpandedRow({
 
       setCommentsDialogOpen(true)
       setActiveView("kpis")
-      // Consumir tab: F5 no debe reabrir Mensajes.
-      consumeCommentsTabParam(router, pathname, searchParams)
+      useFocusIntentStore.getState().consumeTab()
+      useFocusIntentStore.getState().consume()
       return
     }
 
@@ -194,9 +194,6 @@ export function ProcessExpandedRow({
     isTarget,
     tabParam,
     focusSettled,
-    router,
-    pathname,
-    searchParams,
   ])
 
   const setActiveTarget = useActiveCommentContextStore(s => s.setActiveTarget)
diff --git a/src/features/projects/components/expanded-row/project-expanded-row.tsx b/src/features/projects/components/expanded-row/project-expanded-row.tsx
index 0d8539f..e991c3e 100644
--- a/src/features/projects/components/expanded-row/project-expanded-row.tsx
+++ b/src/features/projects/components/expanded-row/project-expanded-row.tsx
@@ -27,7 +27,7 @@ import { ProjectRowActions } from "../actions/project-row-actions"
 import { DetailAssetsEye } from "@/features/detail-assets/components/detail-assets-eye"
 import { CommentHistoryDialog } from "@/features/comments/components/comment-history-dialog"
 import { useFocusSettleStore } from "@/shared/focus/store/focus-settle-store"
-import { consumeCommentsTabParam } from "@/shared/hooks/consume-comments-tab"
+import { useFocusIntentStore } from "@/shared/focus/store/focus-intent-store"
 
 type Props = {
   project: Project
@@ -45,9 +45,9 @@ export function ProjectExpandedRow({
   const router = useRouter()
   const pathname = usePathname()
 
-  const urlProjectId = searchParams.get("projectId")
-  const isTarget = urlProjectId === project.id
-  const tabParam = searchParams.get("tab")
+  const intent = useFocusIntentStore(s => s.intent)
+  const isTarget = intent?.projectId === project.id
+  const tabParam = intent?.tab
 
   const [
     activeView,
@@ -111,7 +111,7 @@ export function ProjectExpandedRow({
     project.id,
   ])
 
-  const urlFocusToken = searchParams.get("focus")
+  const urlFocusToken = intent?.focusToken ?? null
   const settledToken = useFocusSettleStore(s => s.settledToken)
   const focusSettled = !urlFocusToken || settledToken === urlFocusToken
 
@@ -126,10 +126,9 @@ export function ProjectExpandedRow({
       }
 
       setActiveView("comments")
-      // Dialog en todos los breakpoints (paridad con processes).
       setCommentsDialogOpen(true)
-      // Consumir tab: F5 no debe reabrir Mensajes.
-      consumeCommentsTabParam(router, pathname, searchParams)
+      useFocusIntentStore.getState().consumeTab()
+      useFocusIntentStore.getState().consume()
       return
     }
 
@@ -144,9 +143,6 @@ export function ProjectExpandedRow({
     tabParam,
     isMobile,
     focusSettled,
-    router,
-    pathname,
-    searchParams,
   ])
 
   const setActiveTarget = useActiveCommentContextStore(s => s.setActiveTarget)
diff --git a/src/features/tasks/components/expanded-row/task-expanded-row.tsx b/src/features/tasks/components/expanded-row/task-expanded-row.tsx
index af63fe5..163c9c9 100644
--- a/src/features/tasks/components/expanded-row/task-expanded-row.tsx
+++ b/src/features/tasks/components/expanded-row/task-expanded-row.tsx
@@ -29,7 +29,7 @@ import { DetailAssetsEye } from "@/features/detail-assets/components/detail-asse
 import { CommentHistoryDialog } from "@/features/comments/components/comment-history-dialog"
 import { useActiveCommentContextStore } from "@/features/comments/store/active-comment-context-store"
 import { useFocusSettleStore } from "@/shared/focus/store/focus-settle-store"
-import { consumeCommentsTabParam } from "@/shared/hooks/consume-comments-tab"
+import { useFocusIntentStore } from "@/shared/focus/store/focus-intent-store"
 import { isWorkflowCompleted } from "@/features/workflow/selectors/is-completed"
 
 type Props = {
@@ -45,9 +45,9 @@ export function TaskExpandedRow({
   const router = useRouter()
   const pathname = usePathname()
 
-  const urlTaskId = searchParams.get("taskId")
-  const isTarget = urlTaskId === task.id
-  const tabParam = searchParams.get("tab")
+  const intent = useFocusIntentStore(s => s.intent)
+  const isTarget = intent?.taskId === task.id
+  const tabParam = intent?.tab
 
   const [
     activeView,
@@ -79,7 +79,7 @@ export function TaskExpandedRow({
     ? comments.length
     : (task.commentCount ?? 0)
 
-  const urlFocusToken = searchParams.get("focus")
+  const urlFocusToken = intent?.focusToken ?? null
   const settledToken = useFocusSettleStore(s => s.settledToken)
   // Si no hay token en la URL (no vino de un deep-link), no hay nada
   // que esperar — se abre directo, como antes.
@@ -91,16 +91,14 @@ export function TaskExpandedRow({
     }
 
     if (tabParam === "comments") {
-      // Esperar scroll+expand del deep-link; luego abrir dialog en
-      // todos los breakpoints (ya no hay panel inline de mensajes).
       if (!focusSettled) {
         return
       }
 
       setActiveView("comments")
       setCommentsDialogOpen(true)
-      // Consumir tab: F5 no debe reabrir Mensajes.
-      consumeCommentsTabParam(router, pathname, searchParams)
+      useFocusIntentStore.getState().consumeTab()
+      useFocusIntentStore.getState().consume()
       return
     }
 
@@ -115,9 +113,6 @@ export function TaskExpandedRow({
     tabParam,
     isMobile,
     focusSettled,
-    router,
-    pathname,
-    searchParams,
   ])
 
   const setActiveTarget = useActiveCommentContextStore(s => s.setActiveTarget)
diff --git a/src/shared/focus/store/focus-intent-store.ts b/src/shared/focus/store/focus-intent-store.ts
new file mode 100644
index 0000000..825e09b
--- /dev/null
+++ b/src/shared/focus/store/focus-intent-store.ts
@@ -0,0 +1,34 @@
+"use client"
+
+import { create } from "zustand"
+
+/**
+ * Intent de deep-link de un solo uso (memoria).
+ * La URL se limpia al capturar; el trabajo corre contra este store.
+ * F5 → memoria vacía + URL limpia → no re-dirige.
+ */
+export type DeepLinkIntent = {
+  taskId?: string
+  projectId?: string
+  focusToken?: string
+  tab?: string | null
+  key: string
+}
+
+type FocusIntentStore = {
+  intent: DeepLinkIntent | null
+  capture: (intent: DeepLinkIntent) => void
+  consumeTab: () => void
+  consume: () => void
+}
+
+export const useFocusIntentStore = create<FocusIntentStore>(set => ({
+  intent: null,
+  capture: intent => set({ intent }),
+  consumeTab: () =>
+    set(state => {
+      if (!state.intent) return state
+      return { intent: { ...state.intent, tab: null } }
+    }),
+  consume: () => set({ intent: null }),
+}))
diff --git a/src/shared/hooks/clear-entity-focus-params.ts b/src/shared/hooks/clear-entity-focus-params.ts
index 41997db..12acc5f 100644
--- a/src/shared/hooks/clear-entity-focus-params.ts
+++ b/src/shared/hooks/clear-entity-focus-params.ts
@@ -1,15 +1,14 @@
 "use client"
 
 import { useFocusSettleStore } from "@/shared/focus/store/focus-settle-store"
+import { useFocusIntentStore } from "@/shared/focus/store/focus-intent-store"
 
 /**
- * Params de deep-link de entidad (tarea / proyecto / foco).
- * La URL es la fuente de verdad del foco programático.
- * Si el usuario toma el control (otro row, colapsar, nav limpia), se borran.
+ * Params de deep-link de entidad (tarea / proyecto / foco / tab).
+ * Tras captura viven en focus-intent-store; la URL solo transporta la llegada.
  */
 const FOCUS_PARAM_KEYS = ["taskId", "projectId", "focus", "tab"] as const
 
-/** Orígenes de navegación cross-feature (sessionStorage). */
 const ORIGIN_STORAGE_KEYS = [
   "process-origin-task-id",
   "task-origin-project-id",
@@ -29,12 +28,14 @@ function clearOriginButtons() {
   for (const key of ORIGIN_STORAGE_KEYS) {
     sessionStorage.removeItem(key)
   }
-  // Avisa a BackToTask / BackToProject / BackToProcess para que se desmonten sin
-  // depender solo del click del propio botón.
   window.dispatchEvent(new Event("entity-origin-cleared"))
 }
 
-export function clearEntityFocusParams(
+/**
+ * Solo quita params de deep-link de la URL.
+ * No toca el intent store (lo usa useCaptureDeepLink al migrar URL → memoria).
+ */
+export function stripDeepLinkSearchParams(
   router: RouterLike,
   pathname: string,
   searchParams: { toString(): string },
@@ -49,13 +50,6 @@ export function clearEntityFocusParams(
     }
   }
 
-  useFocusSettleStore.getState().reset()
-
-  // URL consumida → no dejar residuos de "← Tarea" / "← Proyecto".
-  if (changed) {
-    clearOriginButtons()
-  }
-
   if (!changed) return
 
   const query = next.toString()
@@ -63,3 +57,31 @@ export function clearEntityFocusParams(
     scroll: false,
   })
 }
+
+/**
+ * Control del usuario (otro row, colapsar, nav):
+ * limpia URL + intent + settle + botones de origen.
+ */
+export function clearEntityFocusParams(
+  router: RouterLike,
+  pathname: string,
+  searchParams: { toString(): string },
+): void {
+  const hadIntent = useFocusIntentStore.getState().intent != null
+  const next = new URLSearchParams(searchParams.toString())
+  let hadParams = false
+  for (const key of FOCUS_PARAM_KEYS) {
+    if (next.has(key)) {
+      hadParams = true
+      break
+    }
+  }
+
+  stripDeepLinkSearchParams(router, pathname, searchParams)
+  useFocusSettleStore.getState().reset()
+  useFocusIntentStore.getState().consume()
+
+  if (hadParams || hadIntent) {
+    clearOriginButtons()
+  }
+}
diff --git a/src/shared/hooks/consume-comments-tab.ts b/src/shared/hooks/consume-comments-tab.ts
deleted file mode 100644
index efe460d..0000000
--- a/src/shared/hooks/consume-comments-tab.ts
+++ /dev/null
@@ -1,25 +0,0 @@
-"use client"
-
-/**
- * Consume `tab=comments` del deep-link una vez abierto el dialog.
- * Deja taskId/projectId/code/focus — solo quita tab para que F5 no reabra Mensajes.
- */
-type RouterLike = {
-  replace: (href: string, options?: { scroll?: boolean }) => void
-}
-
-export function consumeCommentsTabParam(
-  router: RouterLike,
-  pathname: string,
-  searchParams: { toString(): string; get(name: string): string | null },
-): void {
-  if (searchParams.get("tab") !== "comments") return
-
-  const next = new URLSearchParams(searchParams.toString())
-  next.delete("tab")
-
-  const query = next.toString()
-  router.replace(query ? `${pathname}?${query}` : pathname, {
-    scroll: false,
-  })
-}
diff --git a/src/shared/hooks/use-capture-deep-link.ts b/src/shared/hooks/use-capture-deep-link.ts
new file mode 100644
index 0000000..4277fae
--- /dev/null
+++ b/src/shared/hooks/use-capture-deep-link.ts
@@ -0,0 +1,44 @@
+"use client"
+
+import { useEffect } from "react"
+import { usePathname, useRouter, useSearchParams } from "next/navigation"
+
+import { useFocusIntentStore } from "@/shared/focus/store/focus-intent-store"
+import { stripDeepLinkSearchParams } from "@/shared/hooks/clear-entity-focus-params"
+
+/**
+ * URL → intent store (una vez) → strip URL.
+ * No llama clearEntityFocusParams (eso consumiría el intent recién capturado).
+ */
+export function useCaptureDeepLink() {
+  const searchParams = useSearchParams()
+  const router = useRouter()
+  const pathname = usePathname()
+  const capture = useFocusIntentStore(s => s.capture)
+  const intentKey = useFocusIntentStore(s => s.intent?.key ?? null)
+
+  useEffect(() => {
+    const taskId = searchParams.get("taskId") ?? undefined
+    const projectId = searchParams.get("projectId") ?? undefined
+    const focusToken = searchParams.get("focus") ?? undefined
+    const tab = searchParams.get("tab")
+
+    if (!taskId && !projectId && !focusToken && !tab) return
+
+    const key =
+      focusToken ??
+      `entity:${taskId ?? ""}:${projectId ?? ""}:tab:${tab ?? ""}`
+
+    if (intentKey === key) return
+
+    capture({
+      taskId,
+      projectId,
+      focusToken,
+      tab,
+      key,
+    })
+
+    stripDeepLinkSearchParams(router, pathname, searchParams)
+  }, [searchParams, router, pathname, capture, intentKey])
+}
diff --git a/src/shared/hooks/use-focused-row.ts b/src/shared/hooks/use-focused-row.ts
index bbea0cc..07f90ce 100644
--- a/src/shared/hooks/use-focused-row.ts
+++ b/src/shared/hooks/use-focused-row.ts
@@ -14,7 +14,6 @@ type Props = {
 }
 
 const FIND_TIMEOUT_MS = 2500
-const POST_EXPAND_MS = 220
 
 function isScrollable(el: HTMLElement): boolean {
   const { overflowY } = window.getComputedStyle(el)
@@ -53,29 +52,43 @@ function centerInScrollParent(el: HTMLElement) {
   parent.scrollTo({ top, behavior: "auto" })
 }
 
+function afterPaint(cb: () => void): () => void {
+  let raf1 = 0
+  let raf2 = 0
+  let cancelled = false
+  raf1 = window.requestAnimationFrame(() => {
+    raf2 = window.requestAnimationFrame(() => {
+      if (!cancelled) cb()
+    })
+  })
+  return () => {
+    cancelled = true
+    window.cancelAnimationFrame(raf1)
+    window.cancelAnimationFrame(raf2)
+  }
+}
+
 function scrollExpandAndSettle(
   el: HTMLElement,
   isActive: () => boolean,
   expand: () => void,
   onSettled: (() => void) | undefined,
 ): () => void {
-  let disposed = false
-  let timer: number | null = null
+  let cancelPaint: (() => void) | null = null
   const finish = () => {
     useFocusNavStore.getState().end()
     onSettled?.()
   }
-  if (disposed || !isActive()) return () => {}
+  if (!isActive()) return () => {}
   centerInScrollParent(el)
   expand()
-  timer = window.setTimeout(() => {
-    if (disposed || !isActive()) return
+  cancelPaint = afterPaint(() => {
+    if (!isActive()) return
     centerInScrollParent(el)
-    if (!disposed && isActive()) finish()
-  }, POST_EXPAND_MS)
+    if (isActive()) finish()
+  })
   return () => {
-    disposed = true
-    if (timer !== null) window.clearTimeout(timer)
+    cancelPaint?.()
   }
 }
 
@@ -92,7 +105,7 @@ function waitForRow(
   const start = performance.now()
   const tryFind = () => {
     if (cancelled || !isActive()) return
-    const el = root.querySelector<HTMLElement>(selector)
+    const el = root.querySelector(selector) as HTMLElement | null
     if (el) {
       onFound(el)
       return
@@ -110,7 +123,11 @@ function waitForRow(
   }
 }
 
-/** Deep-link / F5 / notificaciones / tab=comments. URL = fuente de verdad. */
+/**
+ * Deep-link: scroll + expand + settle.
+ * focusedId/focusToken vienen del intent store (no de searchParams vivos).
+ * Al perder focusedId no colapsa: la expansión es estado de sesión.
+ */
 export function useFocusedRow({
   focusedId,
   expandedRowId = null,
@@ -150,12 +167,7 @@ export function useFocusedRow({
     if (!focusedId) {
       stopTracking()
       useFocusNavStore.getState().end()
-      useFocusSettleStore.getState().reset()
-      const prev = prevFocusedIdRef.current
       prevFocusedIdRef.current = undefined
-      if (prev && expandedRowIdRef.current === prev) {
-        setExpandedRowIdRef.current(null)
-      }
       return
     }
 
@@ -168,8 +180,12 @@ export function useFocusedRow({
     }
 
     prevFocusedIdRef.current = focusedId
-    useFocusNavStore.getState().start("Dirigiendo…")
-    useFocusSettleStore.getState().reset()
+
+    const animate = Boolean(focusTokenRef.current)
+    if (animate) {
+      useFocusNavStore.getState().start("Dirigiendo…")
+      useFocusSettleStore.getState().reset()
+    }
 
     const selector = `[data-expanded-row-id="${CSS.escape(focusedId)}"]`
     const isActive = () => {
@@ -183,6 +199,14 @@ export function useFocusedRow({
     stopTracking()
     const expand = () => setExpandedRowIdRef.current(focusedId)
 
+    if (!animate) {
+      expand()
+      settleOnly()
+      return () => {
+        stopTracking()
+      }
+    }
+
     const stopWait = waitForRow(
       selector,
       el => {
