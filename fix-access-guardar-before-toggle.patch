diff --git a/src/features/roles/components/role-permissions-page-content.tsx b/src/features/roles/components/role-permissions-page-content.tsx
index 372fd58..2c62b7a 100644
--- a/src/features/roles/components/role-permissions-page-content.tsx
+++ b/src/features/roles/components/role-permissions-page-content.tsx
@@ -482,6 +482,7 @@ export function RolePermissionsPageContent() {
 
               {mode === "usuarios" && selectedUser ? (
                 <div className="flex shrink-0 items-center gap-2">
+                  {/* Acción primaria ANTES del toggle (Editar | Guardar) */}
                   {canEditUser && userPanelView === "profile" && (
                     <PrimaryAction
                       label="Editar"
@@ -489,6 +490,15 @@ export function RolePermissionsPageContent() {
                       onClick={() => setEditUserOpen(true)}
                     />
                   )}
+                  {userPanelView === "exceptions" && (
+                    <PrimaryAction
+                      label={saveLabel}
+                      icon={Save}
+                      isLoading={saving}
+                      onClick={handleSave}
+                      disabled={!dirty || saving}
+                    />
+                  )}
                   <div className="flex items-center gap-1 rounded-xl bg-foreground/5 p-1">
                     <button
                       type="button"
@@ -515,15 +525,6 @@ export function RolePermissionsPageContent() {
                       Excepciones
                     </button>
                   </div>
-                  {userPanelView === "exceptions" && (
-                    <PrimaryAction
-                      label={saveLabel}
-                      icon={Save}
-                      isLoading={saving}
-                      onClick={handleSave}
-                      disabled={!dirty || saving}
-                    />
-                  )}
                 </div>
               ) : (
                 mode === "roles" && (
@@ -619,6 +620,7 @@ export function RolePermissionsPageContent() {
 
               {mode === "usuarios" && selectedUser ? (
                 <div className="flex shrink-0 items-center gap-2">
+                  {/* Acción primaria ANTES del toggle (Editar | Guardar) */}
                   {canEditUser && userPanelView === "profile" && (
                     <PrimaryAction
                       label="Editar"
@@ -626,6 +628,15 @@ export function RolePermissionsPageContent() {
                       onClick={() => setEditUserOpen(true)}
                     />
                   )}
+                  {userPanelView === "exceptions" && (
+                    <PrimaryAction
+                      label={saveLabel}
+                      icon={Save}
+                      isLoading={saving}
+                      onClick={handleSave}
+                      disabled={!dirty || saving}
+                    />
+                  )}
                   <div className="flex items-center gap-1 rounded-xl bg-foreground/5 p-1">
                     <button
                       type="button"
@@ -652,15 +663,6 @@ export function RolePermissionsPageContent() {
                       Excepciones
                     </button>
                   </div>
-                  {userPanelView === "exceptions" && (
-                    <PrimaryAction
-                      label={saveLabel}
-                      icon={Save}
-                      isLoading={saving}
-                      onClick={handleSave}
-                      disabled={!dirty || saving}
-                    />
-                  )}
                 </div>
               ) : (
                 mode === "roles" && (
