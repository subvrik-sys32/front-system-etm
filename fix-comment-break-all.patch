diff --git a/src/features/comments/components/comment-item.tsx b/src/features/comments/components/comment-item.tsx
index 0000000..1111111 100644
--- a/src/features/comments/components/comment-item.tsx
+++ b/src/features/comments/components/comment-item.tsx
@@ -98,7 +98,7 @@
   return (
     <div
       className={cn(
-        "group animate-comment-in flex w-full items-center gap-2",
+        "group animate-comment-in flex w-full min-w-0 items-center gap-2",
         isOwner ? "flex-row-reverse" : "flex-row",
         isReply && !isOwner && "pl-6",
         (isPending || isDeleting) && "opacity-60",
@@ -113,7 +113,8 @@
 
       <div
         className={cn(
-          "flex w-fit max-w-[min(85%,22rem)] flex-col gap-1",
+          // min-w-0: el flex del dialog puede encoger la columna; max-w evita desborde
+          "flex w-fit min-w-0 max-w-[min(85%,22rem)] flex-col gap-1",
           isOwner ? "items-end" : "items-start",
         )}
       >
@@ -162,11 +163,11 @@
         </div>
 
         {/* Burbuja + edit/delete en hover (desktop); siempre en móvil */}
-        <div className="group/bubble relative">
+        <div className="group/bubble relative max-w-full min-w-0">
           <div
             className={cn(
               // Ancho original (columna ya limita a 85%/22rem). Solo el texto parte palabras largas.
-              "w-fit max-w-full min-h-9 rounded-2xl px-3.5 py-2 text-[13px] leading-relaxed shadow-xs",
+              "w-fit max-w-full min-w-0 min-h-9 rounded-2xl px-3.5 py-2 text-[13px] leading-relaxed shadow-xs",
               isOwner
                 ? "bg-foreground text-background"
                 : "bg-muted/80 text-foreground dark:bg-foreground/[0.06]",
@@ -182,7 +183,7 @@
                 )}
               >
                 <Reply size={11} className="mt-0.5 shrink-0 -scale-x-100" />
-                <span className="min-w-0 flex-1 whitespace-pre-wrap break-words [overflow-wrap:anywhere]">
+                <span className="min-w-0 max-w-full flex-1 whitespace-pre-wrap break-all [overflow-wrap:anywhere]">
                   {comment.parent.deletedAt
                     ? "Comentario eliminado"
                     : `${comment.parent.user.name}: ${comment.parent.message || "Foto"}`}
@@ -191,7 +192,7 @@
             )}
 
             {comment.message ? (
-              <p className="whitespace-pre-wrap break-words [overflow-wrap:anywhere]">{comment.message}</p>
+              <p className="max-w-full whitespace-pre-wrap break-all [overflow-wrap:anywhere]">{comment.message}</p>
             ) : null}
 
             {comment.imageUrl ? (
