diff --git a/src/features/detail-assets/components/detail-assets-dialog.tsx b/src/features/detail-assets/components/detail-assets-dialog.tsx
index 7982343..633db8b 100644
--- a/src/features/detail-assets/components/detail-assets-dialog.tsx
+++ b/src/features/detail-assets/components/detail-assets-dialog.tsx
@@ -84,12 +84,26 @@ export function DetailAssetsDialog({
   const [previewPhoto, setPreviewPhoto] = useState<string | null>(null)
   const [note, setNote] = useState("")
 
-  const photos: DetailAsset[] = isTask
+  const ownPhotos: DetailAsset[] = isTask
     ? (taskQ.data?.taskAssets.filter(a => a.kind === "PHOTO") ?? [])
     : (projectQ.data.filter(a => a.kind === "PHOTO") ?? [])
+  // herencia proyecto → tarea (solo lectura en el dialog de tarea)
+  const inheritedPhotos: DetailAsset[] = isTask
+    ? (taskQ.data?.projectAssets?.filter(a => a.kind === "PHOTO") ?? [])
+    : []
+  const photos: DetailAsset[] = isTask
+    ? [...ownPhotos, ...inheritedPhotos]
+    : ownPhotos
+  const ownPhotoCount = ownPhotos.length
   const noteAsset: DetailAsset | undefined = isTask
-    ? taskQ.data?.taskAssets.find(a => a.kind === "NOTE")
+    ? (taskQ.data?.taskAssets.find(a => a.kind === "NOTE") ??
+      taskQ.data?.projectAssets?.find(a => a.kind === "NOTE"))
     : projectQ.data.find(a => a.kind === "NOTE")
+  /** Nota heredada del proyecto: no se edita como nota de tarea. */
+  const noteInherited =
+    isTask &&
+    !taskQ.data?.taskAssets.some(a => a.kind === "NOTE") &&
+    Boolean(taskQ.data?.projectAssets?.some(a => a.kind === "NOTE"))
   const materialLines = taskQ.data?.materialLines ?? []
 
   const loading = isTask ? taskQ.loading : projectQ.loading
@@ -135,9 +149,9 @@ export function DetailAssetsDialog({
                   <section className="flex flex-col gap-2">
                     <div className="flex items-center justify-between">
                       <h3 className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">
-                        Fotos ({photos.length}/2)
+                        Fotos ({isTask ? ownPhotoCount : photos.length}/2)
                       </h3>
-                      {!readOnly && photos.length < 2 && (
+                      {!readOnly && (isTask ? ownPhotoCount : photos.length) < 2 && (
                         <>
                           <input
                             ref={fileRef}
@@ -176,7 +190,12 @@ export function DetailAssetsDialog({
                       />
                     ) : (
                       <div className="flex flex-wrap gap-2">
-                        {photos.map(p => (
+                        {photos.map(p => {
+                          const inherited =
+                            isTask &&
+                            Boolean(p.projectId) &&
+                            !p.taskId
+                          return (
                           <div
                             key={p.id}
                             className="group relative size-20 overflow-hidden rounded-xl bg-muted"
@@ -190,7 +209,12 @@ export function DetailAssetsDialog({
                                 if (p.publicUrl) setPreviewPhoto(p.publicUrl)
                               }}
                             />
-                            {!readOnly && (
+                            {inherited && (
+                              <span className="absolute bottom-1 left-1 rounded bg-background/90 px-1 py-0.5 text-[9px] font-semibold uppercase tracking-wide text-muted-foreground">
+                                Proyecto
+                              </span>
+                            )}
+                            {!readOnly && !inherited && (
                               <button
                                 type="button"
                                 title="Eliminar"
@@ -204,15 +228,23 @@ export function DetailAssetsDialog({
                               </button>
                             )}
                           </div>
-                        ))}
+                          )
+                        })}
                       </div>
                     )}
                   </section>
 
                   <section className="flex flex-col gap-2">
-                    <h3 className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">
-                      Detalle
-                    </h3>
+                    <div className="flex items-center gap-2">
+                      <h3 className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">
+                        Detalle
+                      </h3>
+                      {noteInherited && (
+                        <span className="rounded-md bg-foreground/5 px-1.5 py-0.5 text-[10px] font-semibold uppercase tracking-wide text-muted-foreground">
+                          Del proyecto
+                        </span>
+                      )}
+                    </div>
                     {readOnly ? (
                       noteFromServer ? (
                         <p className="rounded-xl bg-foreground/[0.04] px-3 py-2.5 text-sm leading-relaxed text-foreground">
diff --git a/src/features/detail-assets/hooks/use-detail-assets.ts b/src/features/detail-assets/hooks/use-detail-assets.ts
index 00b35f7..a18c78a 100644
--- a/src/features/detail-assets/hooks/use-detail-assets.ts
+++ b/src/features/detail-assets/hooks/use-detail-assets.ts
@@ -27,6 +27,8 @@ export function invalidateDetailAssetCaches(
     void qc.invalidateQueries({
       queryKey: projectDetailAssetsKey(scope.projectId),
     })
+    // herencia: las tareas del proyecto muestran projectAssets
+    void qc.invalidateQueries({ queryKey: ["detail-assets", "task"] })
   }
   void qc.invalidateQueries({ queryKey: ["tasks"] })
   void qc.invalidateQueries({ queryKey: ["projects"] })
diff --git a/src/features/detail-assets/types.ts b/src/features/detail-assets/types.ts
index bcb3b49..97a09c1 100644
--- a/src/features/detail-assets/types.ts
+++ b/src/features/detail-assets/types.ts
@@ -17,7 +17,13 @@ export type DetailAsset = {
 }
 
 export type TaskDetailAssetsResponse = {
+  /** Fotos/notas propias de la tarea */
   taskAssets: DetailAsset[]
+  /**
+   * Herencia proyecto → tarea: fotos/notas del proyecto.
+   * Visibles en tarea y procesos (read-through, sin copia).
+   */
+  projectAssets?: DetailAsset[]
   materialLines: Array<{
     id: string
     pieces: number
