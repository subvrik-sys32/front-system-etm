diff --git a/src/shared/responsive/layout/app-sidebar.tsx b/src/shared/responsive/layout/app-sidebar.tsx
index 43439d5..f78bf1f 100644
--- a/src/shared/responsive/layout/app-sidebar.tsx
+++ b/src/shared/responsive/layout/app-sidebar.tsx
@@ -1,6 +1,6 @@
 "use client"
 
-import { useState } from "react"
+import { useEffect, useState } from "react"
 import { ProfileDialog } from "@/features/profile"
 import { useSidebarStore, isSidebarHiddenOrMovingOut } from "@/shared/stores/sidebar-store"
 import { cn } from "@/shared/utils/utils"
@@ -62,6 +62,14 @@ export function AppSidebar({ variant = "desktop", open = false }: Props = {}) {
     notifyContentTransitionEnd()
   }
 
+  // Sin animación de width: cerrar el ciclo visual al instante (como drawer móvil)
+  useEffect(() => {
+    if (isDrawer) return
+    if (visualState !== "moving-in" && visualState !== "moving-out") return
+    const id = requestAnimationFrame(() => notifyContentTransitionEnd())
+    return () => cancelAnimationFrame(id)
+  }, [visualState, isDrawer, notifyContentTransitionEnd])
+
   return (
     <>
       <aside
@@ -78,11 +86,10 @@ export function AppSidebar({ variant = "desktop", open = false }: Props = {}) {
           visualState === "moving-out" || visualState === "moving-in"
             ? "will-change-[width]"
             : "will-change-auto",
-          !isDrawer && "transition-[width] duration-200 ease-out",
+          !isDrawer && "transition-none",
           (isDrawer && !isVisible) || isFullyHidden ? "pointer-events-none" : "",
         )}
       >
-        {/* Contenido a ancho expandido fijo: el aside recorta. Iconos no se mueven. */}
         <div className="flex h-full w-full flex-col overflow-hidden pt-1.5 pb-1.5">
           <SidebarHeader collapsed={collapsed} isDrawer={isDrawer} />
 
diff --git a/src/shared/responsive/layout/sidebar/sidebar-profile.tsx b/src/shared/responsive/layout/sidebar/sidebar-profile.tsx
index 35fd6b6..fc7bef8 100644
--- a/src/shared/responsive/layout/sidebar/sidebar-profile.tsx
+++ b/src/shared/responsive/layout/sidebar/sidebar-profile.tsx
@@ -258,7 +258,7 @@ export function SidebarProfile({
           aria-hidden={!profileOpen}
           className={cn(
             "absolute inset-x-0 bottom-full z-0 overflow-hidden rounded-xl bg-popover",
-            "origin-bottom transition-[transform,opacity] duration-200 ease-out",
+            "transition-opacity duration-150 ease-out",
             profileOpen ? "opacity-100" : "pointer-events-none opacity-0",
           )}
           style={{
