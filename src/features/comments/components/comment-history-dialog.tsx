"use client"

// Preparado para futura implementación (Activity Timeline completo).
// No implementar lógica todavía.

type Props = {
  taskId: string
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function CommentHistoryDialog(_props: Props) {

  return null

}