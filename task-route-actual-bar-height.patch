diff --git a/src/features/tasks/components/expanded-row/production/task-route-viewer.tsx b/src/features/tasks/components/expanded-row/production/task-route-viewer.tsx
index de0cdc9..bfd93a2 100644
--- a/src/features/tasks/components/expanded-row/production/task-route-viewer.tsx
+++ b/src/features/tasks/components/expanded-row/production/task-route-viewer.tsx
@@ -14,8 +14,8 @@ type Props = {
   route: ProcessCode[]
   currentProcess?: ProcessCode
   /**
-   * panel — centrado (legacy / móvil en panel).
-   * inline — header expanded desktop.
+   * panel — centrado (legacy / móvil).
+   * inline — misma fila que toggle/actions/barra (desktop).
    */
   variant?: "panel" | "inline"
 }
@@ -36,17 +36,15 @@ export function TaskRouteViewer({
   return (
     <div
       className={cn(
-        "min-w-0 overflow-visible",
-        inline
-          ? "flex max-w-full justify-end"
-          : "flex justify-center",
+        "min-w-0",
+        inline ? "flex max-w-full justify-end" : "flex justify-center",
       )}
     >
       <div
         className={cn(
-          "flex items-center overflow-visible",
+          "flex items-center",
           inline
-            ? "min-w-max flex-nowrap items-center gap-1.5 pt-3.5"
+            ? "min-w-max flex-nowrap gap-1.5"
             : "flex-wrap justify-center gap-2 pt-5",
         )}
       >
@@ -58,26 +56,34 @@ export function TaskRouteViewer({
           const isCurrent = currentProcess === processCode
 
           return (
-            <div key={processCode} className="relative shrink-0 overflow-visible">
-              {isCurrent && (
-                <span
-                  className="pointer-events-none absolute -top-4 left-1/2 z-10 -translate-x-1/2 whitespace-nowrap text-[9px] font-bold uppercase tracking-widest"
-                  style={{ color: process.color }}
-                >
-                  Actual
-                </span>
-              )}
-
+            <div
+              key={processCode}
+              className="relative shrink-0 rounded-lg transition-shadow"
+              style={
+                isCurrent
+                  ? {
+                      boxShadow: `0 0 0 2px var(--background), 0 0 0 4px ${process.color}`,
+                    }
+                  : undefined
+              }
+            >
               <button
                 type="button"
                 disabled={!enabled}
-                title={process.name ?? processCode}
+                title={
+                  isCurrent
+                    ? `${process.label} · Actual`
+                    : process.label
+                }
+                aria-current={isCurrent ? "step" : undefined}
                 onClick={e => {
                   e.stopPropagation()
                   sessionStorage.removeItem("process-origin-code")
                   sessionStorage.removeItem("process-origin-focus-task-id")
                   sessionStorage.setItem("process-origin-task-id", taskId)
-                  router.push(`/processes?code=${processCode}&taskId=${taskId}`)
+                  router.push(
+                    `/processes?code=${processCode}&taskId=${taskId}`,
+                  )
                 }}
                 className="transition-all duration-200 disabled:pointer-events-none"
               >
diff --git a/src/features/tasks/components/expanded-row/task-expanded-row.tsx b/src/features/tasks/components/expanded-row/task-expanded-row.tsx
index 6615fe0..8333b54 100644
--- a/src/features/tasks/components/expanded-row/task-expanded-row.tsx
+++ b/src/features/tasks/components/expanded-row/task-expanded-row.tsx
@@ -63,10 +63,10 @@ function TaskDesktopRouteStrip({ task }: { task: Task }) {
         route={task.route}
         currentProcess={currentStep?.processCode}
       />
-      <div className="flex min-w-[10rem] max-w-[16rem] flex-1 items-center gap-2 rounded-xl bg-foreground/5 px-2.5 py-1.5">
+      <div className="flex h-8 min-w-[11rem] max-w-[18rem] flex-1 items-center gap-2 rounded-lg bg-foreground/5 px-2.5">
         <div className="flex min-w-0 shrink items-center gap-1">
           {StatusIcon && (
-            <StatusIcon size={12} className="shrink-0 text-muted-foreground" />
+            <StatusIcon size={13} className="shrink-0 text-muted-foreground" />
           )}
           <span className="truncate text-[10px] font-bold uppercase tracking-wide text-foreground">
             {statusDef?.label ?? "—"}
