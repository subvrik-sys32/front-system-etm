diff --git a/src/features/tasks/components/expanded-row/production/task-production-panel.tsx b/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
index 4ba9ff9..6a7fb91 100644
--- a/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
+++ b/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
@@ -276,19 +276,10 @@ export function TaskProductionPanel({
                           : `${commentCount} mensajes`
                       }
                       className={cn(
-                        "pointer-events-none absolute -right-1.5 -top-1.5 z-10",
-                        "flex h-4 min-w-4 items-center justify-center",
-                        "rounded-full bg-sky-500 px-1 text-[9px] font-bold leading-none text-white",
-                        "shadow-xs ring-2",
+                        "pointer-events-none absolute -bottom-0.5 -right-0.5 z-10",
+                        "flex h-3.5 min-w-3.5 items-center justify-center",
+                        "rounded-full bg-sky-600/85 px-1 text-[8px] font-bold leading-none text-white",
                       )}
-                      style={{
-                        // Anillo del color del proceso (contraste con el centro, no negro)
-                        boxShadow: `0 0 0 2px ${
-                          isActive || isDone
-                            ? colors.backgroundActive || colors.background
-                            : "color-mix(in srgb, var(--foreground) 22%, var(--background))"
-                        }`,
-                      }}
                     >
                       {commentCount > 99 ? "99+" : commentCount}
                     </span>
@@ -379,18 +370,11 @@ export function TaskProductionPanel({
                 {!isCompact && operator && (
                   <span
                     title={operator.name}
-                    className="mt-0.5 max-w-[5.5rem] truncate rounded-md px-1.5 py-0.5 text-[10px] font-medium shadow-xs"
+                    className="mt-0.5 max-w-[5.5rem] truncate rounded-md px-1.5 py-0.5 text-[10px] font-medium normal-case"
                     style={{
-                      backgroundColor: getBadgeColors(
-                        operator.color || "#64748B",
-                        "subtle",
-                        theme,
-                      ).background,
-                      color: getBadgeColors(
-                        operator.color || "#64748B",
-                        "subtle",
-                        theme,
-                      ).text,
+                      // Un poco más claro que subtle puro; sin border ni ring
+                      backgroundColor: `color-mix(in srgb, ${operator.color || "#64748B"} 38%, var(--background))`,
+                      color: `color-mix(in srgb, ${operator.color || "#64748B"} 55%, var(--foreground))`,
                     }}
                   >
                     {operator.name.trim().split(/\s+/)[0]}
