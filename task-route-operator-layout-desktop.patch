diff --git a/src/features/tasks/components/expanded-row/production/task-production-panel.tsx b/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
index 4388232..d91e6e7 100644
--- a/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
+++ b/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
@@ -294,8 +294,8 @@ export function TaskProductionPanel({
                     </span>
                   )}
 
-                  {/* Operador: esquina opuesta si ya hay contador de mensajes */}
-                  {operator && (
+                  {/* Operador compact: inicial en el círculo */}
+                  {isCompact && operator && (
                     <span
                       title={operator.name}
                       className={cn(
@@ -365,6 +365,33 @@ export function TaskProductionPanel({
                 >
                   {isCompact ? definition.code : definition.label}
                 </span>
+
+                {/* Desktop: nombre bajo la etiqueta, color subtle (no chillón) */}
+                {!isCompact && operator && (
+                  <span
+                    title={operator.name}
+                    className="mt-0.5 max-w-[5.5rem] truncate rounded-md px-1.5 py-0.5 text-[10px] font-medium shadow-xs"
+                    style={{
+                      backgroundColor: getBadgeColors(
+                        operator.color || "#64748B",
+                        "subtle",
+                        theme,
+                      ).background,
+                      color: getBadgeColors(
+                        operator.color || "#64748B",
+                        "subtle",
+                        theme,
+                      ).text,
+                    }}
+                  >
+                    {operator.name.trim().split(/\s+/)[0]}
+                  </span>
+                )}
+                {!isCompact && !operator && hasInvite && (
+                  <span className="mt-0.5 rounded-md bg-amber-500/15 px-1.5 py-0.5 text-[10px] font-medium text-amber-700 dark:text-amber-300/90">
+                    Convocado
+                  </span>
+                )}
               </button>
 
               {!isLast && (
